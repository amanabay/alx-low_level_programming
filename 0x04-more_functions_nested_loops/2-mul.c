#include "main.h"

/**
 * mul -  multiplies two integers and returns the result.
 * @m: first number to be multiplied
 * @n: second number to be multiplied
 *
 * Return: the sum of m and n
 */
int mul(int a, int b)
{
	return (a * b);
}
