#include "main.h"

/**
 * add -  adds two integers and returns the result.
 * @m: first number to be added
 * @n: second number to be added
 *
 * Return: the sum of m and n
 */
int add(int m, int n)
{
	return (m + n);
}
